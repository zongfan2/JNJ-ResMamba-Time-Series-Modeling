# Core ML and Data Processing
# IMPORTANT: NumPy 2.x has breaking changes - pin to 1.x for compatibility
numpy>=1.21.0,<2.0.0
pandas>=1.3.0,<2.3.0
scikit-learn>=1.0.0
scipy>=1.7.0  # For signal processing (butter, filtfilt, stft)

# Deep Learning Framework
torch==2.1.0
mamba-ssm[causal-conv1d]==2.2.2  # Core Mamba architecture

# Visualization and Monitoring
matplotlib>=3.5.0
seaborn>=0.11.0

# Utilities
joblib>=1.1.0  # Model serialization
h5py>=3.7.0  # HDF5 file format support

# Optimization and Hyperparameter Tuning
optuna==4.3.0

# Distributed Training
ray[train]==2.9.0  # Multi-node distributed training
ray[tune]==2.9.0  # Hyperparameter tuning with Ray

# Image Processing (for skimage.measure.block_reduce in DL_helpers)
scikit-image>=0.19.0

# Additional metrics (used in mehdi version)
torcheval>=0.0.6  # Advanced PyTorch metrics
ruptures>=1.1.7  # Changepoint detection (used in DL_helpers_mehdi)

# Data Loading and Future Improvements
# Pin pyarrow to version compatible with NumPy 1.x
pyarrow>=10.0.0,<18.0.0  # Parquet support (already used for caching)
datasets>=2.0.0,<3.0.0  # HuggingFace datasets for future data pipeline improvements

# Optional but Recommended
# pyyaml>=6.0  # Configuration files (uncomment if needed)
# tensorboard>=2.10.0  # Training visualization (uncomment if needed)